<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta
      name="author"
      content="paul  (isdadev) & phillip schlichting (philrico)"
    />
    <title>404 not found</title>
    <style>
      body {
        background-color: rgb(76, 78, 77);
      }

      .error {
        text-align: center;
        margin-top: 20%;
      }

      .error button {
        background-color: black;
        color: white;
        width: 100px;
        height: 50px;
      }
    </style>
  </head>
  <body id="404">
    <div class="error">
      <h1>Seems like the page you requested could not be found</h1>
      <p>(This page may not be pretty, but it works)</p>
      <h2><%= message %></h2>
      <button id="returnButton"><a>Back</a></button>
    </div>
    <script>
      // selects the button to return to the galler
      const returnButton = document.querySelector('#returnButton');

      // adds a simple event-listener that goes back to /gallery
      returnButton.addEventListener('click', () => {
        // relocates the user on click
        window.location = '/gallery';
      });
    </script>
  </body>
</html>
