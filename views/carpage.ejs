<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta
      name="author"
      content="paul  (isdadev) & phillip schlichting (philrico)"
    />
    <link rel="stylesheet" href="/style.css" />
    <title><%= carData['Name'] %></title>
  </head>
  <body id="carpage">
    <header>
      <nav class="desktop">
        <ul>
          <li><a href="/">Home</a></li>
          <li><a href="/gallery">Gallery</a></li>
          <li><a href="/contact">Contact</a></li>
        </ul>
      </nav>
      <div class="nav-mobile">
        <div class="nav-mobile-symbol">
          <img src="/assets/navbar-mobile-bars.png" alt="" width="40px" />
        </div>
        <div class="nav-mobile-links">
          <ul>
            <li><a href="/">Home</a></li>
            <li><a href="/gallery">Gallery</a></li>
            <li><a href="/contact">Contact</a></li>
            <li><a href="/impressum">Impressum</a></li>
            <li><a href="/privacy">Privacy</a></li>
          </ul>
        </div>
      </div>
    </header>

    <main>
      <div class="container">
        <div class="image">
          <button id="control-button-left">
            <img
              src="/assets/arrow-left.png"
              alt="left arrow"
              height="30px"
              class="control-button"
            />
          </button>
          <button id="control-button-right">
            <img
              src="/assets/arrow-right.png"
              alt="right arrow"
              height="30px"
              class="control-button"
            />
          </button>
          <!-- image with the path for the first image -->
          <img src="<%= images[0] %>" alt="" class="displayedCar" />
        </div>
        <div class="info">
          <!-- displays the information about the car -->
          <h2><%= carData['Name'] %></h2>
          <h3><%= carData['Class'] %> <%= carData['Brand'] %></h3>
          <table id="table">
            <!-- loop through each entry in the JSON object and returns key and value -->
            <% Object.entries(carData).forEach(([key, value]) => { %>
            <tr>
              <td><%= key %></td>
              <td><%= value %></td>
            </tr>
            <% }) %>
          </table>
        </div>
      </div>
    </main>
    <footer>
      <p>&copy; 2024 MOSC</p>
      <div class="footer-links">
        <p><a href="/impressum">impressum</a></p>
        <p><a href="/privacy">privacy</a></p>
      </div>
    </footer>

    <script>
      // imageLinks is an array containg all the paths for the images
      const imageLinks = <%- JSON.stringify(images) %>;
    </script>

    <!-- import of javascript -->
    <script src="../scripts/image-slider-carpage.js"></script>
    <script src="../scripts/functionality.js"></script>
  </body>
</html>
